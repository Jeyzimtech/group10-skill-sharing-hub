import '../config_keys.dart';

class AgoraConfig {
  static const String appId = ConfigKeys.agoraAppId; 
  static const String token = ""; // Use null or "" for testing if token is not enabled in Agora console
}
