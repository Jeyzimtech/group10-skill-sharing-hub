import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette
  static const Color primary = Color(0xFF00E5A0); // Vibrant Mint/Green
  static const Color secondary = Color(0xFF0B1E3A); // Deep Navy
  
  // Background Palette
  static const Color background = Color(0xFF0B1E3A); 
  static const Color surface = Color(0xFF122240); // Lighter Navy for cards
  
  // Neutral Palette
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF94A3B8);
  
  // Functional Colors
  static const Color accentWhite = Colors.white;
  static const Color success = Color(0xFF00E5A0);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
}
